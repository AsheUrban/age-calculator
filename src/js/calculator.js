// import $ from "jquery";





export default function calculatePlanetAge(earthAge, planetYr) {
  if(planet === "Mercury") {
    (earthAge * planetYr);
  }
}

let planet = "Mercury";
const planetYr = .24;
const earthAge = 80;
let newPlanetAge = 0;